/**
 * Admin console feature modules (embedded in operations-web at /admin/*).
 * Source lives in apps/admin-web until a full physical move is completed.
 */
export const ADMIN_CONSOLE_ROOT = 'apps/admin-web/src/app';
